# Properties configuration for evaluation.
# Similar structure to marathon/data/properties.py -- may be unified later.

# Which properties get normalized by number of atoms
DEFAULT_NORMALIZATION = {
    "energy": "atom",
    "stress": "atom",
}

# Example: train a model using the `marathon.hermes` infrastructure

Soon, this will contain an example on how to use the `grain`-based infrastructure to train models at (some modest amount of) scale.

For a preview, please see `examples/extra/hermes/run.py`, which is currently slightly out of sync with the state of the code, but contains most of the functionality.
